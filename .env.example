# Example environment variables for local development.
# Copy this file to `.env` and fill real values. Do NOT commit your `.env` with real secrets.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=apilocations
DB_PORT=5432

# Example DATABASE_URL for Prisma (Postgres). If you run docker-compose from the host,
# keep `localhost` as the host. If Prisma runs in a container, use the service name `db`.
# Replace the placeholder values above when you create your real .env file.
DATABASE_URL=postgresql://POSTGRES_USER:POSTGRES_PASSWORD@localhost:5432/apilocations?schema=public
